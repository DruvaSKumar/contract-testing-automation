# ============================================================
# Contract: should_return_400_when_updating_user_with_invalid_email.yml
# ============================================================
# AUTO-GENERATED by AI Agent — NEGATIVE/ERROR scenario
# Endpoint: PUT /api/users/{id}
# Scenario: should return 400 when email format is invalid
#
# This contract verifies proper error handling.
# It ensures the Provider returns correct status codes and
# a consistent error response body when things go wrong.
#
# To regenerate: python main.py generate --overwrite
# ============================================================

description: Returns 400 when email format is invalid
name: should_return_400_when_updating_user_with_invalid_email
request:
  method: PUT
  url: /api/users/1
  headers:
    Content-Type: application/json
  body:
    name: Test User
    email: not-a-valid-email
    role: USER
response:
  status: 400
  headers:
    Content-Type: application/json
  body:
    status: 400
    message: 'Validation failed: please check the field errors for details'
    timestamp: '2026-01-15T10:30:00'
    fieldErrors:
      email: Email must be a valid email address
  matchers:
    body:
    - path: $.status
      type: by_regex
      value: '400'
    - path: $.message
      type: by_regex
      value: .+
    - path: $.timestamp
      type: by_regex
      value: .+
