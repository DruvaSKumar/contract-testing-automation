# ============================================================
# Contract: should_return_404_when_updating_user_not_found.yml
# ============================================================
# AUTO-GENERATED by AI Agent — NEGATIVE/ERROR scenario
# Endpoint: PUT /api/users/{id}
# Scenario: should return 404 when resource not found
#
# This contract verifies proper error handling.
# It ensures the Provider returns correct status codes and
# a consistent error response body when things go wrong.
#
# To regenerate: python main.py generate --overwrite
# ============================================================

description: Returns 404 when user not found
name: should_return_404_when_updating_user_not_found
request:
  method: PUT
  url: /api/users/999
  headers:
    Content-Type: application/json
  body:
    name: Test User
    email: test@example.com
    role: USER
response:
  status: 404
  headers:
    Content-Type: application/json
  body:
    status: 404
    message: 'User not found with id: 999'
    timestamp: '2026-01-15T10:30:00'
    fieldErrors: null
  matchers:
    body:
    - path: $.status
      type: by_regex
      value: '404'
    - path: $.message
      type: by_regex
      value: .+
    - path: $.timestamp
      type: by_regex
      value: .+
