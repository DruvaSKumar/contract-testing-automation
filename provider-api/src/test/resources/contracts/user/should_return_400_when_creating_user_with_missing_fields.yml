# ============================================================
# Contract: should_return_400_when_creating_user_with_missing_fields.yml
# ============================================================
# AUTO-GENERATED by AI Agent — NEGATIVE/ERROR scenario
# Endpoint: POST /api/users
# Scenario: should return 400 when required fields are missing
#
# This contract verifies proper error handling.
# It ensures the Provider returns correct status codes and
# a consistent error response body when things go wrong.
#
# To regenerate: python main.py generate --overwrite
# ============================================================

description: Returns 400 when required fields are missing
name: should_return_400_when_creating_user_with_missing_fields
request:
  method: POST
  url: /api/users
  headers:
    Content-Type: application/json
  body: {}
response:
  status: 400
  headers:
    Content-Type: application/json
  body:
    status: 400
    message: 'Validation failed: please check the field errors for details'
    timestamp: '2026-01-15T10:30:00'
    fieldErrors:
      name: Name is required and cannot be blank
  matchers:
    body:
    - path: $.status
      type: by_regex
      value: '400'
    - path: $.message
      type: by_regex
      value: .+
    - path: $.timestamp
      type: by_regex
      value: .+
